language: android
jdk: oraclejdk8

branches:
  only:
    - master

script: "./gradlew clean assembleDebug"

android:
  components:
      - platform-tools
      - tools
      - build-tools-28.0.3
      - android-28
      - extra-android-m2repository
